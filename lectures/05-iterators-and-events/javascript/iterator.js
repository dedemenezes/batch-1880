const students = ['Luna', 'Neville', 'Hermione']

// RUBY ITERATOR
// students.each do |student|
//   // do your thing
// end

students.forEach((student) => {
  // For each one of the elements
  console.log(student.toUpperCase());
})
